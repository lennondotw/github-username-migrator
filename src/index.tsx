#!/usr/bin/env bun
/**
 * GitHub Username Migrator
 *
 * CLI tool to scan all git repositories and migrate GitHub username in remote URLs.
 */

import { render, Text } from 'ink';

const App: React.FC = () => {
  return <Text>GitHub Username Migrator - Coming Soon</Text>;
};

render(<App />);
